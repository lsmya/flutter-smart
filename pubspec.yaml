name: flutter_smart
description: Flutter toolkit.
version: 0.0.2
homepage: https://github.com/lsmya/flutter_smart

topics:
  - toolkit

environment:
  sdk: ^3.10.4
  flutter: '>=3.3.0'

dependencies:
  cached_network_image: ^3.4.1
  event_bus: ^2.0.1
  flutter:
    sdk: flutter
  flutter_screenutil: ^5.9.3
  shared_preferences: ^2.5.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

platforms:
  android:
  ios:
  linux:
  macos:
  windows:
  web:

flutter:
  assets:
    - assets/images/